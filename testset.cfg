from plptest import *

TestConfig = c = {}

test = Test(
  name = 'KWS',
  commands = [
    Shell('clean', 'make clean KWS_BITS=16'),
    Shell('build', 'make all KWS_BITS=16'),
    Shell('run',   'make run platform=gvsoc KWS_BITS=16')
  ],
  timeout=1000000,
)

c['tests'] = [ test ]